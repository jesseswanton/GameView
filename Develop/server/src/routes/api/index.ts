import { Router } from 'express';
import {   } from './ .js';
import {   } from './ .js';

const router = Router();

router.use('/ ', volunteerRouter);
router.use('/ ', workRouter);

export default router;
